import React from 'react';
import {MovieInfo} from "../../components/MovieInfo/MovieInfo";

const MoviePage = () => {
    return (
        <div>
            <MovieInfo/>
        </div>
    );
};

export {MoviePage};
